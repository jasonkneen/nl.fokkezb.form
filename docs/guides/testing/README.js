Ext.data.JsonP.testing({"guide":"<h1 id='testing-section-testing'>Testing</h1>\n\n<p>The <a href=\"https://github.com/FokkeZB/nl.fokkezb.form/tree/test\">test</a> branch has an app that can be used for testing the widget. This guide explains how to use it.</p>\n\n<h3 id='testing-section-preparing-'>Preparing </h3>\n\n<p>The repo has a <code>Gruntfile.js</code> for <a href=\"http://gruntjs.com/getting-started\">Grunt</a> to replace the app's <code>app/widgets/nl.fokkezb.form</code> with a copy from the <code>master</code> branch. The configuration assumes to find this branch at <code>../master</code>.</p>\n\n<p>Install Grunt and the dependencies via:</p>\n\n<pre><code>$ sudo npm i -g grunt\n$ sudo npm install\n</code></pre>\n\n<h3 id='testing-section-running'>Running</h3>\n\n<p>To replace the app's copy of the widget and build the app for the simulator simply execute:</p>\n\n<pre><code>$ grunt\n</code></pre>\n\n<h3 id='testing-section-automated-tests'>Automated tests</h3>\n\n<p>This branch is simply an Alloy project using as much of the options. At some point I probably should add automated tests using e.g. <a href=\"https://github.com/tonylukasavage/ti-mocha\">ti-mocha</a>, but for now this will do.</p>\n","title":"Testing"});